{
  "scripts": {
    "start": "wintersmith preview",
    "build": "wintersmith build",
    "deploy": "aws s3 sync build/ s3://stories.data4america.org/",
    "deploy-test": "aws s3 sync build/ s3://test.data4america.org/"
  },
  "dependencies": {
    "moment": "2.3.x",
    "typogr": "0.5.x",
    "underscore": "1.4.x",
    "wintersmith": "^2.2.1",
    "wintersmith-nap": "git://github.com/dumbmatter/wintersmith-nap.git"
  },
  "private": "true"
}
