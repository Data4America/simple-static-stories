
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + ' - '}

block header
  a(href='/')
    img.logo(src=contents.img['header-small.png'].url)
  div.container
    include author
    h1= page.title
    if page.metadata.author
      p.author
        | #{ 'By ' }
        mixin author(page.metadata.author)
    hr

block content
  article.article
    section.content!= typogr(page.html).typogrify()
    if page.metadata.code || page.metadata.data
      hr
      if page.metadata.data
        p.data-link
          | #[a(href=page.metadata.data) Download the original data]
      if page.metadata.code
        p.code-link
          | #[a(href=page.metadata.code) Fork the code on Github]
  hr

block prepend footer
  div.nav
    a(href=contents.index.url) « Full blog
