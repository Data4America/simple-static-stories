extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + ' - '}

block content
  div.container.content-block
    h1= page.title
    article.article
      section.content!= typogr(page.html).typogrify()